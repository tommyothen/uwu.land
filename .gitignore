node_modules
.turbo
dist
.next
.open-next
.wrangler
.dev.vars
coverage
*.tsbuildinfo

# local-only deploy runbook (not for publication)
docs/DEPLOY.md

# Local Claude Code config/skills — not tracked
.claude/

# DSAR exports hold personal data and must never be committed
dsar-*.json
